This folder contains a small launcher for the Streamlit active learning demo.

To run the app from the repository root:

```bash
streamlit run frontend/app.py
```

The app reads CSV files from the `data/` folder. Use the UI to select featurisers, kernels and protocols.
